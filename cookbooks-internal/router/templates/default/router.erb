# incoming ssh traffic
-A FWR -p tcp -m tcp --dport 22 -j ACCEPT

# incoming dhcp requests
-A INPUT -p udp --dport 67 -j ACCEPT
-A INPUT -p udp --dport 68 -j ACCEPT

# incoming tftp traffic
-A INPUT -p udp --dport 69 -j ACCEPT

# nat me some traffic
#-P FORWARD ACCEPT
#--table nat -A POSTROUTING -o eth1 -j MASQUERADE
-A POSTROUTING -t nat -s 172.16.33.0/24 -o eth0 -j MASQUERADE
